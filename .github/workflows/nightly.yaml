name: nightly

on:
  schedule:
    - cron: "0 0 * * *" # nightly At 00:00
  workflow_dispatch:

jobs:
  nightly-build:
    name: nightly-build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      # - uses: actions/cache@v3
      #   with:
      #     path: |
      #       ~/.cache/go-build
      #       ~/go/pkg/mod
      #     key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
      #     restore-keys: |
      #       ${{ runner.os }}-go-

      # - uses: wangyoucao577/go-release-action@v1.35
      #   with:
      #     github_token: ${{ secrets.GITHUB_TOKEN }}
      #     goos: linux
      #     goarch: amd64
      #     pre_command: export GODEBUG=http2client=0
      #     build_command: make build-nightly
      #     release_tag: nightly
      #     release_name: nightly
      #     goversion: 1.19
      #     overwrite: true
      #     retry: 10

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build nightly image
        uses: docker/build-push-action@v4
        with:
          context: .
          tags: "ehco1996/ehco:nightly"
          file: build/Dockerfile
          cache-from: type=registry,ref=ehco1996/ehco:buildcache
          cache-to: type=registry,ref=ehco1996/ehco:buildcache,mode=max
